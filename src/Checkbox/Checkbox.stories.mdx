import { useState } from 'react';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { actions } from '@storybook/addon-actions';
import Checkbox from '../Checkbox/Checkbox';

<Meta title='Components/Checkbox' component={Checkbox} />

# Checkbox

The `<Checkbox />` component is an equivalent to the typical `<input type="checkbox">` element. It should be used to collect single values for submission 
in a form.

<br />
<br />

## Default

<Canvas>
  <Story name='Default'>
    {() => {
      const [checkedState, setCheckedState] = useState(false);
      return <Checkbox id="default" name="default" label="Default" value="default" checked={checkedState} onChange={(e) => setCheckedState(e.target.checked)} />
    }}
  </Story>
</Canvas>

## Required

<Canvas>
  <Story name='Required'>
    {() => {
      const [checkedState, setCheckedState] = useState(false);
      return <Checkbox id="required" name="required" label="Required" value="required" checked={checkedState} required onChange={(e) => setCheckedState(e.target.checked)} />
    }}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name='Disabled'>
    {() => {
      const [checkedState, setCheckedState] = useState(false);
      return <Checkbox id="disabled" name="disabled" label="Disabled" value="disabled" checked={checkedState} disabled onChange={(e) => setCheckedState(e.target.checked)} />
    }}
  </Story>
</Canvas>

## Prop Definitions

<ArgsTable of={Checkbox} />