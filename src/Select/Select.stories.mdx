import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { actions } from '@storybook/addon-actions';
import Select from '../Select/Select';
import { useState } from 'react';

<Meta title='Components/Select' component={Select} />

# Select

Lorem ipsum.

## Default

<Canvas>
  <Story name='Default'>
    {() => {
      const [selectedItems, setSelectedItems] = useState([]);
      return (
        <Select label="Default" placeholder="Select an option..." options={[{itemLabel: 'Pizza', value: 'pizza'}, {itemLabel: 'Hot dog', value: 'hot dog'}, {itemLabel: 'Churro', value: 'churro'}, {itemLabel: 'Frozen yogurt', value: 'frozen yogurt'}]} multiSelect width={25} onChange={(value) => setSelectedItems(value)} value={selectedItems} />
      );
    }}
  </Story>
</Canvas>

## Prop Definitions